# Xcode
**/xcuserdata/
**/*.xcuserstate
DerivedData/
.DS_Store

# Generated by `xcodegen generate` from watch/project.yml, which is the source
# of truth. Tracking it meant every checkout's first generate produced a diff
# nobody made — and the two had already drifted apart in both directions.
watch/Diem/Info.plist
watch/DiemWidget/Info.plist

# Written by `sh Scripts/ring-gallery.sh` — pictures of the ring, not sources.
watch/.ring-gallery/

# Wrangler's local state and build cache.
.wrangler/

# Skills installed per-machine, not vendored.
.claude/
